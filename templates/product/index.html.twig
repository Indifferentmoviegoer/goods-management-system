{% extends 'base.html.twig' %}

{% block title %}Импорт товаров{% endblock %}

{% block body %}
    <div class="container container-fluid">
        <div class="row">
            <div class="col-lg-8">
                {{ form_start(form, {'multipart': true }) }}
                <div class="form-group">
                    {{ form_label(form.upload_file) }}
                    {{ form_widget(form.upload_file) }}
                    {{ form_errors(form.upload_file) }}
                </div>
                <br>
                <div class="form-group">
                    {{ form_widget(form.send, {'label': "Загрузить файл", 'attr' : { 'class': 'btn btn-primary' }}) }}
                </div>
                {{ form_rest(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
